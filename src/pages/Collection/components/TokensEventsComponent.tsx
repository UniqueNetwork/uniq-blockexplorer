import React, { FC } from 'react';

interface TokensEventsComponentProps {
  className?: string
}

const TokensEventsComponent: FC<TokensEventsComponentProps> = (props) => {
  const { ...rest } = props;

  return (
    <>
    </>
  );
};

export default TokensEventsComponent;
