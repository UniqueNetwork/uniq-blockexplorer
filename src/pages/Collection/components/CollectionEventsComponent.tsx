import React, { FC } from 'react';

interface CollectionEventsComponentProps {
  className?: string
}

const CollectionEventsComponent: FC<CollectionEventsComponentProps> = (props) => {
  const { ...rest } = props;

  return (
    <>
    </>
  );
};

export default CollectionEventsComponent;
